"""Parser plugins. Every submodule here is auto-imported at registry load, so
dropping a ``@register``-decorated file in this directory is the entire process
of adding a parser."""
from __future__ import annotations
